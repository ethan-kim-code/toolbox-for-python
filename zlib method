import zlib

# Original data
data = b"Hello, Hello, Hello! This is a test of compression."

# Compress
compressed = zlib.compress(data)
print("Compressed:", compressed)

# Decompress
decompressed = zlib.decompress(compressed)
print("Decompressed:", decompressed.decode())
